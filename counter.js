module.exports.count = function(arr) {
    return 'there is ' + arr.length + ' elements inside this array';
};

module.exports.adder = function(a, b) {
    return `this is the result of a + b ${a + b} :)`;
};

module.exports.pi = 3.1415;
